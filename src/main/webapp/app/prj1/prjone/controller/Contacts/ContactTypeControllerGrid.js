Ext.define('Buzzor.prj1.prjone.controller.Contacts.ContactTypeControllerGrid',{extend : 'Ext.app.ViewController',alias : 'controller.ContactTypeControllerGrid',init : function() {},onGridItemClick : function(me, record, item, index, e,eOpts) {var treePanel = this.view.up().up().up().down('#ContactTypeTree');var foundNode = this.findChild(treePanel.getRootNode(), 'primaryKey', record.data.primaryKey);treePanel.setSelection(foundNode);},renderFormValue : function(val, me){debugger;store = this.view.up().down('#ContactType').down('#'+me.column.dataIndex+'').store;return store.findRecord('primaryKey',val).data.primaryDisplay;},setData : function(data){ContactTypeControllerStore = this.view.store;ContactTypeControllerStore.removeAll();ContactTypeControllerStore.add(data);ContactTypeControllerStore.setPageSize(30);ContactTypeControllerStore.sort('primaryDisplay', 'ASC');},findChild : function(node, key, value) {var dNode = node;if (node.data.bConfig != null && node.data.bConfig[key] == value){return node;}else if (node.childNodes) {for (var index = 0; index < node.childNodes.length; index++) {dNode = this.findChild(node.childNodes[index],key, value);if (dNode) {return dNode;}}} else{return null;}}});